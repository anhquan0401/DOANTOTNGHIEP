@model IEnumerable<WebApplication1.ViewModels.LichSuHoaDonVM>

@{
    ViewData["Title"] = "Mới đặt hàng";
    // var currentTab = ViewBag.CurrentTab as string ?? "NewOrder";
    // var NewOrderCount = ViewBag.NewOrderCount as int? ?? 0;
}

<!-- Header -->
<div class="container-fluid page-header py-5">
    <h1 class="text-center text-white display-6">Mới đặt hàng</h1>
    <ol class="breadcrumb justify-content-center mb-0">
        <li class="breadcrumb-item"><a href="#">Home</a></li>
        <li class="breadcrumb-item"><a href="#">Pages</a></li>
        <li class="breadcrumb-item active text-white">Mới đặt hàng</li>
    </ol>
</div>

<!-- Navigation Tabs -->
@await Component.InvokeAsync("HoaDon", new { currentTab = ViewBag.CurrentTab ?? "NewOrder" })

<!-- Order List -->
<div class="container mt-5">
    @foreach (var hoaDon in Model)
    {
        <div class="card shadow-sm mb-4">
            <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
                <h5 class="mb-0">Mã hóa đơn: @hoaDon.MaHd</h5>
                <a asp-action="Details" asp-route-id="@hoaDon.MaHd" class="btn btn-light btn-sm">Xem chi tiết</a>
            </div>
            <div class="card-body">
                <table class="table table-bordered text-center align-middle">
                    <thead class="table-dark">
                        <tr>
                            <th>Tên hàng hóa</th>
                            <th>Hình ảnh</th>
                            <th>Số lượng</th>
                            <th>Đơn giá</th>
                            <th>Giảm giá</th>
                            <th>Thành tiền</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var chiTiet in hoaDon.ChiTietHds)
                        {
                            <tr>
                                <td class="align-middle">@chiTiet.TenHh</td>
                                <td class="align-middle">
                                    <img src="~/Hinh/HangHoa/@chiTiet.Hinh" class="img-thumbnail" style="width: 80px; height: 80px;">
                                </td>
                                <td class="align-middle">@chiTiet.SoLuong</td>
                                <td class="align-middle">@chiTiet.DonGia.ToString("#,##").Replace(',', '.')$</td>
                                <td class="align-middle">@chiTiet.GiamGia.ToString("C")</td>
                                <td class="py-5">
                                    <div class="py-3 border-bottom border-top">
                                        <p> <span id="finalTotal">@chiTiet.ThanhTien.ToString("#,##").Replace(',', '.') đ</span> đ</p>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
</div>

